# Lead Management System

Мінімалістичний веб-сайт на чистому PHP для управління лідами через API.

## 📋 Опис проекту

Двосторінковий сайт з формою для створення лідів та таблицею для перегляду статусів лідів.

## 🚀 Функціонал

### Сторінка 1: Додавання ліда (index.php)
- Форма з обов'язковими полями:
  - Ім'я (firstName)
  - Прізвище (lastName)
  - Телефон (phone)
  - Email (email)
- Автоматичне визначення IP користувача
- Автоматичне визначення домену (landingUrl)
- Відправка даних через API метод `addlead`
- Статичні поля:
  - box_id = 28
  - offer_id = 5
  - countryCode = GB
  - language = en
  - password = qwerty12

### Сторінка 2: Статуси лідів (statuses.php)
- Таблиця зі статусами лідів
- Колонки: ID, Email, Status, FTD
- Фільтр за датами (dateFrom, dateTo)
- Отримання даних через API метод `getstatuses`
- За замовчуванням показує ліди за останні 30 днів

## 🛠️ Технічний стек

- **PHP** (чистий PHP без фреймворків)
- **cURL** для API запитів
- **CSS** для стилізації
- **HTML5** для розмітки

## 📁 Структура проекту

```
progect_cite-php/
├── index.php          # Сторінка з формою додавання ліда
├── statuses.php       # Сторінка зі статусами лідів
├── api.php            # API функції та конфігурація
├── style.css          # Стилі
└── README.md          # Документація
```

## ⚙️ Налаштування

### 1. Конфігурація API

Відкрийте файл `api.php` та вкажіть правильний URL API:

```php
define('API_BASE_URL', 'https://api.example.com'); // ЗАМІНИТИ на реальний URL
define('API_TOKEN', 'ba67df6a-a17c-476f-8e95-bcdb75ed3958');
```

### 2. Вимоги до хостингу

- PHP 7.4 або вище
- Розширення cURL увімкнено
- Підтримка HTTPS (рекомендовано)

## 🌐 Деплой на безкоштовний хостинг

### Варіант 1: InfinityFree
1. Зареєструйтеся на [infinityfree.net](https://infinityfree.net)
2. Створіть новий аккаунт хостингу
3. Завантажте всі файли через File Manager або FTP
4. Відкрийте ваш домен

### Варіант 2: 000webhost
1. Зареєструйтеся на [000webhost.com](https://www.000webhost.com)
2. Створіть новий вебсайт
3. Завантажте файли через File Manager
4. Перейдіть на ваш сайт

### Варіант 3: Vercel (з налаштуванням PHP)
1. Встановіть Vercel CLI
2. Створіть `vercel.json` для налаштування PHP
3. Виконайте команду `vercel deploy`

## 📤 Завантаження на GitHub/GitLab

### GitHub

```bash
# Ініціалізація Git репозиторію
git init

# Додавання файлів
git add .

# Коміт
git commit -m "Initial commit: Lead Management System"

# Підключення до віддаленого репозиторію
git remote add origin https://github.com/ваш-username/progect_cite-php.git

# Відправка на GitHub
git push -u origin main
```

### GitLab

```bash
# Ініціалізація Git репозиторію
git init

# Додавання файлів
git add .

# Коміт
git commit -m "Initial commit: Lead Management System"

# Підключення до віддаленого репозиторію
git remote add origin https://gitlab.com/ваш-username/progect_cite-php.git

# Відправка на GitLab
git push -u origin main
```

## 🔧 Локальне тестування

### За допомогою вбудованого PHP сервера:

```bash
cd c:\Users\artem\Desktop\progect_cite-php
php -S localhost:8000
```

Відкрийте браузер: `http://localhost:8000`

## 📝 API Endpoints

### addlead
- **Метод:** POST
- **Endpoint:** `/addlead`
- **Параметри:**
  - firstName (string, обов'язково)
  - lastName (string, обов'язково)
  - phone (string, обов'язково)
  - email (string, обов'язково)
  - box_id (int)
  - offer_id (int)
  - countryCode (string)
  - language (string)
  - password (string)
  - ip (string)
  - landingUrl (string)

### getstatuses
- **Метод:** GET
- **Endpoint:** `/getstatuses`
- **Параметри:**
  - dateFrom (string, формат: Y-m-d)
  - dateTo (string, формат: Y-m-d)

## 🔐 Безпека

- Валідація всіх вхідних даних
- Екранування HTML виводу через `htmlspecialchars()`
- Використання підготовлених запитів
- HTTPS для API комунікації
- Token авторизація

## 📱 Адаптивний дизайн

Сайт повністю адаптований для мобільних пристроїв та планшетів.

## 🎨 Особливості дизайну

- Сучасний gradient фон
- Плавні анімації та переходи
- Інтуїтивна навігація
- Кольорові бейджі статусів
- Responsive дизайн

## 🐛 Усунення проблем

### API не відповідає
- Перевірте URL API в файлі `api.php`
- Перевірте правильність токену
- Перевірте, що cURL розширення увімкнено

### Помилки відображення
- Перевірте, що файл `style.css` завантажується
- Перевірте консоль браузера на помилки

### Дані не відправляються
- Перевірте валідацію форми
- Перевірте network вкладку в DevTools
- Перевірте логи PHP

## 📞 Контакти

Якщо у вас виникли питання, створіть issue в репозиторії проекту.

## 📄 Ліцензія

MIT License

---

**Примітка:** Не забудьте замінити `API_BASE_URL` в файлі `api.php` на реальний URL з документації API!
